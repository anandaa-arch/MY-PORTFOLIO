/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // add domains if you host external images
  },
  experimental: {
    typedRoutes: true
  }
};

export default nextConfig;
